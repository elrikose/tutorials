# Find

```
# Find all files
find . -type f

# Find all folders
find . -type d

# Find all text files
find . -type f -name  "*.txt"

# Find all files to a certain folder depth (eg. 4)
find . -maxdepth 4 -type f
```